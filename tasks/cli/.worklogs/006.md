## 2026-03-07T00:00:00Z

Implemented CLI entry point in `apps/cli/cmd/viewmd/main.go`.

**Approach:** Matched the Python `main()` behavior exactly — port parsing from
`os.Args`, `net.Listen` for early port-conflict detection, startup banner
matching Python format, and SIGINT/SIGTERM handling with `context`-based
graceful shutdown.

**Completed:**
- [x] Port argument parsing (default 8000, validates numeric input)
- [x] HTTP server bound with handler from task 005
- [x] Port-in-use detection via `net.Listen` before server start
- [x] Startup banner matching Python output format
- [x] SIGINT/SIGTERM graceful shutdown printing "Shutting down..."
- [x] VERSION constant defined as "0.1.0"
- [x] Unit tests for banner output and port-in-use detection

**Files changed:**
- `apps/cli/cmd/viewmd/main.go` — full rewrite with server startup
- `apps/cli/cmd/viewmd/main_test.go` — new test file

All tests pass (`make test`), Go vet clean.
