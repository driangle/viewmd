## 2026-03-08T00:00:00Z

Implemented `--watch` / `-w` flag for auto-refresh on file changes.

**What was done:**
- Added `fsnotify` dependency for filesystem watching
- Created `internal/watcher/` package with recursive dir watching, debouncing (100ms), ignore patterns, and auto-adding new subdirectories
- Created SSE endpoint (`/-/events`) in `internal/handler/sse.go` that streams `data: reload` messages
- Added `WatchMode` to all render data structs and injected watch script into all 5 HTML templates via `_watch.html` partial
- Wired up `-watch`/`-w` CLI flags in `main.go`
- Added tests for watcher (file change detection, debouncing, ignore patterns, new subdirectory detection) and SSE endpoint
- Updated docs: `README.md`, `docs/reference/cli.md`, `docs/getting-started/quick-start.md`
- Added Documentation section to `CLAUDE.md` for future feature documentation guidance

**Verification:** `make build`, `make test`, and `make lint` all pass.
