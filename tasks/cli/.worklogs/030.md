## 2026-03-08T10:00:00Z

Starting implementation of full-text search with keyboard-driven UI.

**Approach:** Add a `/search` API endpoint to the handler package that returns JSON results. Build the search UI directly into the directory template with pure JavaScript — no framework needed given the existing keyboard navigation patterns.

**Plan:**
1. Backend: Add search handler with recursive file walking, name/content matching
2. Frontend: Search bar in directory template, auto-activate on typing, keyboard navigation
3. Wire up debounced fetch requests to the search API

## 2026-03-08T18:00:00Z

Task completed. All acceptance criteria verified.

**Completed:**
- [x] Backend search endpoint (`/-/search`) with name, content, and both modes
- [x] Recursive directory walking with hidden dir exclusion and file size limits
- [x] Keyboard-driven search UI: `/` or typing activates, Tab cycles modes, arrows navigate, Enter opens, Escape closes
- [x] Content match snippets displayed with matching line context
- [x] Debounced requests (200ms), result limit (50), case-insensitive matching
- [x] Comprehensive test suite (10 tests) covering all modes and edge cases
- [x] Follow-up UI fix for search bar layout (commit 82c90ec)
