# Introduction

viewmd is a minimal, read-only file viewer for the browser. It serves a local directory over HTTP and renders each file type as nicely as possible:

- **Markdown** — full rendered HTML with styled typography
- **Code/text** — syntax-highlighted display
- **Images** — inline rendering
- **Directories** — clean file listing with keyboard navigation
- **Other files** — download link or "can't preview" message

## Design Philosophy

**View everything, render what you can, manage nothing.**

viewmd is not a file manager — it doesn't upload, edit, rename, or delete. It's a read-only viewer that focuses on rendering files beautifully in the browser.

## Next Steps

- [Installation](/getting-started/installation) — install viewmd via Homebrew or from source
- [Quick Start](/getting-started/quick-start) — start browsing files in under a minute
